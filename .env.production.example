# Production Environment
# Configure with actual production values
# These values should be set via GitHub Secrets for CI/CD
# NEVER commit secrets - always use GitHub Secrets or environment variable injection

NODE_ENV=production

# Database - Production
# Set via GitHub Secret: PRODUCTION_DATABASE_URL
DB_USER=${PRODUCTION_DB_USER}
DB_PASSWORD=${PRODUCTION_DB_PASSWORD}
DB_NAME=barber_booking_prod
DB_PORT=5432
DATABASE_URL=${PRODUCTION_DATABASE_URL}

# Redis - Production
REDIS_PORT=6379
REDIS_URL=${PRODUCTION_REDIS_URL}

# API - Production
API_PORT=3001

# Web - Production
WEB_PORT=3000
VITE_API_URL=https://api.example.com

# Authentication - JWT Tokens (Production)
JWT_ACCESS_SECRET=${PRODUCTION_JWT_ACCESS_SECRET}
JWT_REFRESH_SECRET=${PRODUCTION_JWT_REFRESH_SECRET}
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d
