# Environment Configuration
NODE_ENV=development
PORT=3001

# Database Configuration
DATABASE_URL=postgresql://devuser:devpassword@localhost:5432/appdb

# JWT Configuration
# Both secrets should be long, random strings in production (min 32 characters)
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production

# Token Expiry Configuration
# Access token should be short-lived (e.g., 15 minutes)
# Refresh token can be longer (e.g., 7 days)
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# Optional: Email configuration for notifications
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password

# Optional: Redis configuration for rate limiting
REDIS_URL=redis://localhost:6379
